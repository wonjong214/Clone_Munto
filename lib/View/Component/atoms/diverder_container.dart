import 'package:flutter/material.dart';

Container boldDivider = Container(
  height: 12,
  width: double.infinity,
  decoration: BoxDecoration(
    color: Colors.grey.shade200,
  ),
);

const Divider thinDivider = Divider(color: Color(0xffa9a9a9), height: 0.2, thickness: 0.3,);